global:
  scrape_interval: 15s

rule_files :
  - "alert_rules.yml"

scrape_configs:

  - job_name: "ragops-api"

    metrics_path: /metrics

    static_configs:
      - targets:
          - ragops-rag-api.onrender.com